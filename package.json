{
  "name": "jwt-server",
  "private": true,
  "scripts": {
    "build": "bun build ./script.js --outdir ./dist",
    "start": "bun run server.js",
    "dev": "bun --watch run server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
